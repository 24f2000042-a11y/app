import React, { useState, useEffect } from 'react'; import { getTasks, addTask, deleteTask, toggleTask } from './api'; const App = () => { const [tasks, setTasks] = useState([]); const [taskInput, setTaskInput] = useState(''); useEffect(() => { fetchTasks(); }, []); const fetchTasks = async () => { const data = await getTasks(); setTasks(data); }; const handleAddTask = async () => { await addTask({ name: taskInput }); setTaskInput(''); fetchTasks(); }; const handleDeleteTask = async (id) => { await deleteTask(id); fetchTasks(); }; const handleToggleTask = async (id) => { await toggleTask(id); fetchTasks(); }; return (<div><h1>Task App</h1><input value={taskInput} onChange={(e) => setTaskInput(e.target.value)} /><button onClick={handleAddTask}>Add Task</button><ul>{tasks.map(task => (<li key={task._id}>{task.name} <button onClick={() => handleToggleTask(task._id)}>Toggle</button> <button onClick={() => handleDeleteTask(task._id)}>Delete</button></li>))}</ul></div>); }; export default App;