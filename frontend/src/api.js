import axios from 'axios'; const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000'; export const getTasks = async () => { const response = await axios.get(`${API_URL}/api/tasks`); return response.data; }; export const addTask = async (task) => { await axios.post(`${API_URL}/api/tasks`, task); }; export const deleteTask = async (id) => { await axios.delete(`${API_URL}/api/tasks/${id}`); }; export const toggleTask = async (id) => { await axios.patch(`${API_URL}/api/tasks/${id}`); };