const Item = require('../models/Item'); exports.getTasks = async (req, res) => { const tasks = await Item.find(); res.json(tasks); }; exports.addTask = async (req, res) => { const newTask = new Item(req.body); await newTask.save(); res.status(201).json(newTask); }; exports.deleteTask = async (req, res) => { await Item.findByIdAndDelete(req.params.id); res.status(204).send(); }; exports.toggleTask = async (req, res) => { const task = await Item.findById(req.params.id); task.completed = !task.completed; await task.save(); res.json(task); };